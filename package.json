{
  "name": "ai-factory-scroll",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "check": "node --check app.js && node --check timeline.js && node --check scripts/build.mjs && node --check scripts/serve.mjs && node --test scripts/*.test.mjs",
    "preview": "node scripts/serve.mjs --dist"
  },
  "engines": {
    "node": ">=22"
  }
}
